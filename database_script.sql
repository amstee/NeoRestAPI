CREATE TABLE    user(
  id            INTEGER PRIMARY KEY, 
  email         VARCHAR(255),
  password      VARCHAR(64),
  fname         VARCHAR(255),
  lname         VARCHAR(255),
  birthday      VARCHAR(255)      
);